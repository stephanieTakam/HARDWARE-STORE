<!-- include header -->
<%- include('include/_header') %>
<!-- /include header -->

  <!-- Main Site -->
  <main id="site-main">
    <div class="container">
        <div class="box-nav d-flex justify-between">
           <div class="filter">
               <a href="/product"><i class="fas fa-angle-double-left"></i> All Products</a>
           </div>
        </div>
        <div class="form-title text-center">
            <h2 class="text-dark">Update Product</h2>
            
        </div>

        <!-- add user form -->
         <!-- form handling -->
 <form method="POST" id="update_product">
    <div class="new_user">
        <div class="form-group">
            <label for="name" class="text-light">Code</label>
            <input type="hidden" name="id" value="<%= user._id %>">
            <input type="text" name="code" value="<%= user.code %>" placeholder="Product Code">
        </div>
        <div class="form-group">
            <label for="name" class="text-light">Name</label>
            <input type="hidden" name="id" value="<%= user._id %>">
            <input type="text" name="name" value="<%= user.name %>" placeholder="Product name">
        </div>
        <div class="form-group">
            <label for="Email" class="text-light">Price</label>
            <input type="text" name="price" value="<%= user.price%>" placeholder="Price">
        </div>
        <div class="form-group">
            <label for="name" class="text-light">Quantity</label>
            <input type="hidden" name="id" value="<%= user._id %>">
            <input type="text" name="quantity" value="<%= user.quantity %>" >
        </div>
    
        <div class="form-group">
            <label for="name" class="text-light">Details</label>
            <input type="hidden" name="id" value="<%= user._id %>">
            <input type="text" name="details" value="<%= user.details %>" >
        </div>

        <div class="form-group">
            <button type="submit" class="btn text-dark update">Save</button>
        </div>

    </div>
</form>
       
    </div>
</main>
<!-- /Main Site -->

<!-- include footer -->
<%- include('include/_footer') %>
<!-- /include footer -->